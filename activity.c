//write a program to calculate area of rectangle usin arithmetics operations
#include<stdio.h>
int main()
{
    int width;
    int length;
    int area;
    printf("enter the width l:\n");
   scanf("%d",&width); 
   printf("enter the length:\n");  
    scanf("%d",&length);
    area=l*b;
    printf("area of rectangle is%d",area);
    return 0; 

}